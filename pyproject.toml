[project]
name = "steamfriends"
version = "0.1.0"
description = "让你的steam好友和WeChat一样，被删除后留下记录。"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.1",
    "requests>=2.32.5",
    "tabulate>=0.9.0",
]
